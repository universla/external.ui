
{
  "name": "external-ui",
  "version": "1.0.0",
  "description": "Directorio de bots tipo top.gg",
  "scripts": {
    "dev": "serve public",
    "start": "serve public",
    "vercel-build": "echo 'Building static files' && cp -r public dist"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": "20.x"
  }
}
